{% set handle = formHandle|default("storybook") %}

{% if craft.app.plugins.pluginEnabled('freeform') %}

    {% set formProperties = formProperties|default({}) %}

    {{ freeform.form(handle, formProperties,).render() }}

{% endif %}
